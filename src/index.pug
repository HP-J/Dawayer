doctype html

mixin icon(name)
  svg.icon
    use(xlink:href=`./icons.svg#${name}`)

html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    meta(http-equiv='X-UA-Compatible' content='ie=edge')
    meta(http-equiv='Content-Security-Policy' content=`script-src 'unsafe-inline'`)

    link(rel='stylesheet' href='./renderer/styles/albums.css')
    link(rel='stylesheet' href='./renderer/styles/artists.css')
    link(rel='stylesheet' href='./renderer/styles/body.css')
    link(rel='stylesheet' href='./renderer/styles/controlBar.css')
    link(rel='stylesheet' href='./renderer/styles/controls.css')
    link(rel='stylesheet' href='./renderer/styles/fonts.css')
    link(rel='stylesheet' href='./renderer/styles/menu.css')
    link(rel='stylesheet' href='./renderer/styles/queue.css')
    link(rel='stylesheet' href='./renderer/styles/tippy.css')
    link(rel='stylesheet' href='./renderer/styles/tracks.css')

  body.fastforward

    .controlBar.container
      .controls.container
        .seekBar.container
          .seekBar.played
          .seekBar.remaining
        .playButton.paused
          .pause(data-tippy='Pause')
            +icon('pause')
          .play(data-tippy='Pause')
            +icon('play')
        .previousButton(data-tippy='Previous Track')
          +icon('next')
        .nextButton(data-tippy='Next Track')
          +icon('next')
        .rewindButton
          +icon('forward')
          .rewindTime
        .forwardButton
          +icon('forward')
          .forwardTime
        .shuffleButton.shuffled
          .shuffle(data-tippy='Shuffled queue')
            +icon('shuffle')
          .linear(data-tippy='Normal queue')
            +icon('linear')
        .repeatButton.looping
          .loop(data-tippy='Looping the queue')
            +icon('loop')
          .repeat(data-tippy='Repeating the track')
            +icon('repeat')
          .singular(data-tippy='Playing the queue once')
            +icon('singular')
        .volumeButton
          .normal
            +icon('volume')
          .mute
            +icon('mute')
      .queue.container
        .queue.playing.card
          .queue.playing.cover(style='background-image: url(../test-materials/Dina1.jpeg);')
          .queue.playing.artist Dina El-Wedidi
          .queue.playing.title Dawayer (Circles) [ft. Mazaher]
        .queue.text Up Next
        .queue.tracks
          .queueItem.container
            .queueItem.index -1
            .queueItem.artist Dina El-Wedidi
            .queueItem.title Yohadethoni El Shagar (Trees Adress Me)
      .controlBar.background

    .menu.container
      .menuItem.wrapper.playing
        .menuItem.container
          +icon('playing')
      .menuItem.wrapper.local.selected
        .menuItem.container
          +icon('albums')
          +icon('music')
          +icon('artist')
      .menuItem.wrapper.options
        .menuItem.container
          +icon('settings')
      .menu.background

    .playing.background

    .pages.container
      .page.container.playing
      .page.container.local
        .page.extended.local
          .albums.wrapper
            .albums.container
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              .album.wrapper.placeholder
                .album.container
                  .album.cover
                  .album.card
                    .album.background
                    .album.title
                    .album.text
                    .album.artist
              //- .album.wrapper
                .album.container
                  .album.cover(style='background-image: url(../test-materials/Dina2.png);')
                  .album.card
                    .album.background
                    .album.title Slumber
                    .album.text by
                    .album.artist  Dina El-Wedidi
          .tracks.wrapper
          .artists.wrapper
      .page.container.options

    .submenu.container.playing
      .submenu.button.playing Playing Now

    .submenu.container.local
      .submenu.button.albums Albums
      .submenu.button.tracks Tracks
      .submenu.button.artists Artists

    .submenu.container.options
      .submenu.button.options Options

    .volumeBar.container
      .volumeBar.played
      .volumeBar.remaining

  script.
    window.onerror = function(message, url, line, column, error)
    {
      require('electron').ipcRenderer.send('rendererError', error.stack || error);
    };
    require('./renderer/renderer.js');
