{
  "name": "dawayer",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "npx babel src --out-dir public --ignore node_modules --source-maps --copy-files --watch",
    "compile": "npx babel src --out-dir public --ignore node_modules --source-maps --copy-files",
    "clean": "rm -r ./public/",
    "test": "eslint ./src ./test && nyc mocha --require babel-core/register --require babel-polyfill"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/herpproject/Dawayer.git"
  },
  "author": "Herp Project",
  "license": "MIT",
  "dependencies": {
    "electron": "^3.0.0-beta.5",
    "fs-extra": "^7.0.0",
    "howler": "^2.0.14"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-eslint": "^8.2.6",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.3.0",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "puppeteer": "^1.2.0",
    "puppeteer-to-istanbul": "^1.2.2"
  }
}
