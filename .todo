main:
  ☐ caching storage data depends on what is needed to be loaded for a faster start-up time and what can be read using lazy-loading later

graphics:

playback:
  ☐ playing and pausing a song
  ☐ update progress in the seek bar
  ☐ allow the user to change the position of the playback using the seek bar
  ☐ change the global volume using the volume bar

  ☐ next button always skips to next song
  ☐ previous button goes back only if playback time is less than 5 seconds else resets the playback to the start of the audio file
  
  ☐ queue sorts audio files (if same album sort by track number) then sort by title then sort by filename